# Styler Review: 3.01 - Dashboard Design (v2.2 Audit)

**Agent**: `ui-visual-critic` (The Styler)
**Date**: 2025-12-23
**Status**: ⚠️ **Needs Polish**

---

## 1. The Roast (Critique)

### 🚨 Critical Fixes (The Enforcer)
*   **The Emoji Indecision**:
    *   You claim v2.1 uses Material Symbols, yet the spec is full of "`📋 (clipboard emoji) OR Material Symbol assignment`".
    *   **Decision**: Stop sitting on the fence. **KILL THE EMOJIS.** The checklist says "Replace emoji with Material Symbols Rounded". The spec should reflect *only* the final decision.
    *   Specific culprits:
        *   `📋` (Plan Badge) -> Use `description`
        *   `✏️` (Custom Badge) -> Use `edit`
        *   `✅` (Completion) -> Use `check_circle` (Verify this!)
        *   `😴` (Rest Day) -> Use `bedtime` (Material Symbol)

*   **Color Discipline**:
    *   We are still seeing `#FF5555` in the Empty State gradient.
    *   **Fix**: Use our brand tokens. If you need a "Red/Coral", use `#FF6B6B` (Error Accent) or `#FF9999` (Coral Core). Don't invent `#FF5555`.

### ⚡ The Vibe Check (The Spark)
*   **Rest Day Card**: The "Skip to Next Day" flow is solid UX, but visually? `#7986CB` (Indigo)?
    *   *Critique*: It feels like a different app. Shapyfy is Mint/Coral/Dark. Suddenly we have Indigo?
    *   *Suggestion*: Can we use a "Dark Mode" styling for Rest Days? Dark gray card `SurfaceContainerHigh` with white text? It implies "Lights out / Resting". Indigo feels like "Facebook Blue".

---

## 2. The Verdict

**⚠️ Major Concerns**
1.  **Specification Cleanliness**: Remove all "OR Emoji" lines. The developer will copy-paste the emoji if you leave it there.
2.  **Rest Day Palette**: Reconsider the indigo. It breaks our strict color system.

**✅ Strengths**
*   The **Liquid Container** specs (v2.1) are significantly better. The removal of confetti and reduction of bubble count shows maturity.
*   **Recent Workouts** section is a great addition for density.

**Action Items**:
1.  **Purge Emojis**: Update `3.01-dashboard.md` to strictly specify Material Symbols (names: `description`, `edit`, `bedtime`, `check_circle`) and delete emoji references.
2.  **Fix Empty State Colors**: Replace `#FF5555` with brand colors.
3.  **Review Rest Day Colors**: Propose an on-brand alternative (e.g., Grayscale or Dark Mode inverse) instead of random Indigo.
